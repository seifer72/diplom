= form_for @scientific_work do |f|
  - if @scientific_work.errors.any?
    #error_explanation
      %h2= "#{pluralize(@scientific_work.errors.count, "error")} prohibited this scientific_work from being saved:"
      %ul
        - @scientific_work.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :general_plan_id
    = f.number_field :general_plan_id
  .actions
    = f.submit 'Save'
