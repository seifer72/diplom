= form_for @approbation_dissertation do |f|
  - if @approbation_dissertation.errors.any?
    #error_explanation
      %h2= "#{pluralize(@approbation_dissertation.errors.count, "error")} prohibited this approbation_dissertation from being saved:"
      %ul
        - @approbation_dissertation.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :dissertation_id
    = f.number_field :dissertation_id
  .actions
    = f.submit 'Save'
